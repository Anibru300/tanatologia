# Avance 2026-08-16

Sincronización con repo remoto, limpieza de deuda técnica media/baja y verificación de videollamadas.

## Hecho hoy

### 1. Sincronización del repositorio
- `git pull origin main` trajo 10 commits nuevos.
- Se corrigió un artifact en `platform/web/package-lock.json` (cambio de stat, contenido idéntico).
- Verificación inicial: build, lint y 21 tests en verde.

### 2. Problemas de nivel medio resueltos
| Issue | Archivo(s) | Fix |
|---|---|---|
| Orden de carga de scripts no garantizado | `assets/js/components.js` | `async = false` en `siteConfig.js`, `chatbot.js` y `atencion-bot.js`. |
| Errores silenciosos en notificaciones | `NotificationBell.tsx` | Revertir estado optimista + mostrar advertencia visible si falla marcar leídas. |
| Error silencioso al borrar archivo de Storage | `verificationService.ts` | Ahora lanza error visible si no se puede eliminar el archivo. |
| Warning de lint `only-export-components` | `AuthProvider.tsx` | Refactor: `AuthContext.tsx`, `types.ts`, `useAuth.ts`; imports actualizados en ~22 archivos. |
| Vitest no fijado | `package.json` | Agregado como `devDependency` (^3.2.7); script usa `vitest run`. |

### 3. Menús "Pronto" reducidos
- Nuevo componente compartido `AccountSettings` en `features/account/AccountSettings.tsx`.
- `AdminConfig`: dejó de ser `ComingSoon`; ahora permite cambiar correo y contraseña.
- `PatientSettings`: nueva página; agregada al menú del paciente y al router.
- `ProfessionalEarnings`: dejó de ser `ComingSoon`; ahora muestra tarifa de sesión ($400), membresía trimestral ($300) y canales de contacto reales.

**Estado de badges "Pronto":**
- Paciente: **0**
- Profesional: **2** (Membresía, Recursos)
- Admin: **5** (Finanzas, Pagos, Reportes, CMS, Soporte)

### 4. Workflow de deploy mejorado
- Archivo `.github/workflows/deploy-app.yml` separado en dos jobs: `build` (permiso `read`) y `deploy` (permiso `write`).
- El job `deploy` solo corre en push a `main` y usa artifact entre jobs.
- Se eliminó el comentario de "versión propuesta".
- Actions actualizadas a v7.

### 5. Dependabot seguros mergeados
- `actions/checkout@v7`
- `actions/setup-node@v7`
- `stefanzweifel/git-auto-commit-action@v7`
- `react-dom` ^19.2.8 + `@types/react-dom` ^19.2.4
- `react-router-dom` ^7.18.2
- `oxlint` ^1.78.0

`package-lock.json` se regeneró limpio. Build, lint y tests siguen en verde.

### 6. Videollamadas verificadas
- El usuario probó videollamadas y funcionan correctamente.
- Se identificaron limitantes documentados: uso de `meet.jit.si` público, sin lobby/contraseña, sin E2EE, sin SLA, PII compartida con 8x8, link permanente, sin previsualización de video ni reconexión automática.
- Opciones de pago investigadas: Jitsi as a Service (JaaS) tiene plan Developer gratis hasta 25 MAU; planes pagos desde ~$99 USD/mes por 300 MAU.

## Verificación final
```bash
cd platform/web
npm run lint   # 0 warnings, 0 errors
npm run build  # OK
npm run test   # 21 passed
```

## Pendientes para mañana
1. **Buzón MX** (`hola@somos-calma.com`): acción del cliente en Hostinger (crear reenvío o buzón).
2. Menús "Pronto" restantes: decidir si se implementan, se ocultan o se dejan con badge.
3. Evaluar migración de videollamadas a JaaS/servidor propio para privacidad y SLA.
4. Actualizaciones major de Dependabot pendientes: `tailwindcss` v4 y `typescript` v7 (riesgo de breaking changes).
5. Warning menor de Vite: `__dirname` en `vite.config.ts` → migrar a `import.meta.dirname`.
