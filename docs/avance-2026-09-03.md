# Avance 2026-09-03 — Desbloqueo de Supabase, migraciones aplicadas, E2E y legales Beta

Continuación de `avance-2026-09-02.md`. El bloqueo del DNS se resolvió (el proyecto
`qjwebikgrqtotqfipeqt` volvió a resolver y responder; las credenciales existentes siguen válidas).

## Hecho hoy

### Infraestructura Supabase
- **Migraciones 010 y 011 aplicadas en Cloud** vía `supabase db query --linked -f` y verificadas:
  tabla `feedback` operativa (10 columnas); triggers `audit_professional_profiles`,
  `audit_professional_documents`, `audit_appointments`, `audit_feedback` y
  `set_feedback_role_trigger` presentes en `pg_trigger` y escribiendo en `audit_logs`.
- **Edge Function `contact-form` redeplegada** (ahora con rate-limit 5/min por IP + honeypot).
  Verificada viva: responde 200 `{"ok":true}`. Ojo: el envío a `hola@somos-calma.com` aún
  depende del pendiente #10 (sin MX el correo se acepta en Resend pero puede rebotar en destino).

### Pruebas E2E (Bloques 3–6, 10 y auth)
- `scripts/test-auth-flow.mjs`: **15/15**. Se corrigió el test (bug del propio script): filtraba
  `professional_profiles` sin `profile_id=eq.<propio>`, así que `json[0]` era del directorio
  (otro profesional `verified`) y no del recién registrado (`pending`). Las 2 "fallas" de la
  corrida anterior eran el test, no la BD.
- `scripts/test-core-flows.mjs` (nuevo, 22 pruebas): registro/login con cuentas
  `e2e-core-*@test.somos-calma.com`, slots con EXCLUDE anti-traslape, booking íntegro,
  rechazo de cita fuera de slot (009) y de doble-reserva (EXCLUDE 009), `get_booked_slots`,
  notificaciones de agenda/confirmación/cancelación, notas clínicas (profesional escribe,
  paciente no ve — RLS), feedback (insert con `profile_id`, trigger fija `role`, lectura propia,
  DELETE en `new`), contact-form. **22/22**. Incluye el mismo filtro por perfil propio
  (el directorio verificado es legible por RLS — `json[0]` no garantía el perfil propio).
- Profesional de prueba verificado vía SQL con trigger `enforce_professional_profile_update_restrictions`
  deshabilitado/rehabilitado en la misma sesión (el trigger bloquea hasta al DBA).
- **Limpieza:** usuarios de prueba eliminados (`DELETE FROM auth.users WHERE email LIKE ...`),
  0 cuentas de prueba restantes.

### Legales versión "sin pagos" (Beta)
- `pages/terminos.html`: nueva sección 7 "Fase Beta gratuita y futuros planes de pago"
  (gratis mientras dure la Beta; cambios con 30 días de aviso; nada se cobra sin consentimiento
  expreso); definición "Beta" sustituye a "Membresía"; 6.4 sin comisiones; eliminadas
  obligaciones de pago (11.1/11.2) y tope de responsabilidad ajustado a servicios gratuitos (12.2).
- `pages/cancelacion.html`: reescrita como "Política de Cancelación" (sin reembolsos):
  cancelación de Cuenta gratuita, sesiones (24 h, no show, tolerancia 15 min), baja del
  directorio, sección de reembolsos reducida a "no proceden durante la Beta".
- `pages/aviso-privacidad.html`: datos bancarios/RFC solo voluntarios y para pagos futuros;
  finalidad de pagos condicionada a futuro aviso; eliminada transferencia a cobranza;
  eliminado "procesamiento de pagos" de proveedores.

### Verificación local
- `npm run lint`: 0 warnings, 0 errors (114 archivos)
- `npm run test`: 25/25
- `npm run build`: OK

## Pendientes
1. Cliente (Hostinger): crear reenvío/buzón de `hola@somos-calma.com` (MX inexistente, #10).
2. Commit + push de: scripts de pruebas (2), 3 legales, `AGENTS.md` y este documento.
3. Cuando haya volumen: migración de videollamadas a JaaS (pendiente pre-existente #13).
