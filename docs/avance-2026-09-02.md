# Avance 2026-09-02 — Preparación Beta gratuita (Bloques 2, 7 y 9 parciales)

Sesión de reconversión a **Beta gratuita** sobre la versión real del repo (Desktop). Nota: la
carpeta de Google Drive quedó descartada como copia de trabajo (versión antigua de junio);
el repo remoto y esta copia local son la versión vigente.

## Hecho hoy

### Bloque 2 — Monetización eliminada de la experiencia
- App React: eliminadas `QuotePage`, `PatientPayments`, `ProfessionalEarnings`,
  `ProfessionalMembership`; retiradas 9 rutas y entradas de menú (Cotización, Pagos,
  Ingresos, Membresía, Finanzas, Pagos a profesionales, Reportes); cards "Ingresos $0"
  → tarjeta "Beta gratuita"; precios fuera de `BookAppointment`, `TherapistDirectory` y
  `ProfessionalProfile` (precios ahora opcionales en `UpdateProfessionalProfileInput`);
  FAQ de pagos reescrita.
- Sitio estático: `index.html` (hero/meta/stats/pricing), `pages/membresias.html`
  reescrita como "Beta gratuita", `pacientes.html`, `profesionales.html` (metadata
  "pagos seguros" fuera), `matching.html`, `login.html`, chatbot (sin precios/pagos/
  promociones), nav/footer ("Membresías" → "Beta gratuita"), `siteConfig.js` (pricing
  comentado como reservado).
- Conservado para fase futura (documentado en `docs/beta-monetizacion-diferida.md`):
  `siteConfig.pricing`, tabla `quotes`, precios en `professional_profiles`, columnas
  financieras de `appointments`, `platform_settings.commission_percent`,
  `formatCurrency()`, páginas admin ComingSoon en disco sin ruta/menú.
- Bug pre-existente corregido en `assets/js/main.js`: llave `}` faltante que impedía
  ejecutar TODO el JS del sitio (FAQ, stats, validaciones).

### Bloque 7 — Sistema de feedback (nuevo, no existía)
- Migración `010_feedback.sql`: tabla `feedback` + índices + trigger de `role` +
  RLS (propio + admin; DELETE propio solo en `new`).
- Feature `platform/web/src/features/feedback/`: types, service, formulario y lista
  compartidos, páginas de paciente/profesional/admin, rutas y menús, 4 tests nuevos.
- Admin: filtros por rol/tipo/estado/fecha + cambio de estado con notas.

### Bloque 9 (parcial, SQL/edge listos para aplicar)
- Migración `011_security_hardening.sql`: triggers que escriben `audit_logs`
  (professional_profiles, professional_documents, appointments, feedback) + Storage
  `professional-documents` restringido a rol profesional/admin.
- Edge Function `contact-form`: rate-limit por IP (5/min) añadido al honeypot.

### Herramienta de pruebas
- `platform/web/scripts/test-auth-flow.mjs`: 13 pruebas E2E de auth/RLS contra
  Supabase Cloud (registro paciente/profesional, degradación de role=admin,
  anti-auto-verificación, anti-escalación de role, RLS propio vs ajeno, login,
  anónimo). **Pendiente de ejecutar** (ver bloqueo abajo).

## Bloqueo encontrado 🔴
El proyecto Supabase `qjwebikgrqtotqfipeqt` **no resuelve en DNS** (NXDOMAIN,
comprobado con 8.8.8.8 y 1.1.1.1; `supabase.com` sí resuelve). Posibles causas:
proyecto eliminado o ref cambiado (un proyecto pausado seguiría resolviendo DNS).
El sitio desplegado usa ese ref en el formulario de contacto → probablemente roto en
prod también. **Pendiente del cliente:** verificar/restaurar el proyecto o entregar
nuevas credenciales (.env + secrets de GitHub Actions + `index.html`).

## Verificación local (sin backend)
- `npm run lint`: 0 warnings, 0 errors (113 archivos)
- `npm run test`: 25/25 (21 existentes + 4 de feedback)
- `npm run build`: OK

## Pendientes
1. Cliente: estado del proyecto Supabase (restaurar o nuevas credenciales).
2. Aplicar migraciones 010 y 011 en Cloud + redeploy de `contact-form`.
3. Ejecutar `scripts/test-auth-flow.mjs` y las pruebas E2E de los Bloques 3–6 y 10.
4. Legales: versión "sin pagos" (decisión del cliente).
5. Commit + push de todos los cambios (16 archivos app + 9 sitio + docs/migraciones).
